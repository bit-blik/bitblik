name: Telegram Bot Docker build

on:
  push:
    branches: [main]
    paths:
      # Keep this list aligned with packages/telegram_bot/Dockerfile. Tests,
      # docs, examples, and local deployment files do not affect the image.
      - 'packages/telegram_bot/bin/**'
      - 'packages/telegram_bot/lib/**'
      - 'packages/telegram_bot/pubspec.yaml'
      - 'packages/telegram_bot/Dockerfile'
      - 'packages/core/lib/**'
      - 'packages/core/pubspec.yaml'
      # The Docker build copies every workspace member manifest before pub get.
      - 'packages/app/pubspec.yaml'
      - 'packages/cli/pubspec.yaml'
      - 'packages/coordinator/pubspec.yaml'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.dockerignore'
      - '.github/workflows/telegram-bot-build.yml'
  pull_request:
    paths:
      - 'packages/telegram_bot/bin/**'
      - 'packages/telegram_bot/lib/**'
      - 'packages/telegram_bot/pubspec.yaml'
      - 'packages/telegram_bot/Dockerfile'
      - 'packages/core/lib/**'
      - 'packages/core/pubspec.yaml'
      - 'packages/app/pubspec.yaml'
      - 'packages/cli/pubspec.yaml'
      - 'packages/coordinator/pubspec.yaml'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.dockerignore'
      - '.github/workflows/telegram-bot-build.yml'
  release:
    types: [published]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1

      # Flutter is needed to resolve workspace dependencies that include the
      # BitBlik app package.
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        working-directory: ./packages/telegram_bot
        run: dart analyze --no-fatal-warnings

      - name: Run tests
        working-directory: ./packages/telegram_bot
        run: dart test

      - name: Build server executable
        working-directory: ./packages/telegram_bot
        run: dart build cli -t bin/server.dart -o build/server

  build:
    needs: test
    env:
      REGISTRY: ghcr.io
      IMAGENAME: telegram-bot
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - uses: actions/checkout@v4
        name: Check out code

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        # Fork PR tokens are read-only. Build those images without publishing.
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGENAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./packages/telegram_bot/Dockerfile
          push: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=telegram-bot
          cache-to: type=gha,scope=telegram-bot,mode=max
